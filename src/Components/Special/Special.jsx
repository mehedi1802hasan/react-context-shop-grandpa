import React, { useContext } from 'react';
import { RingContext } from '../Grandpa/Grandpa';

const Special = ({ring}) => {
  
    return (
        <div>
            <h4>specialll</h4>
            <p>Ring: {ring}</p>
            
        </div>
    );
};

export default Special;